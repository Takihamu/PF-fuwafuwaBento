<h2>コース一覧・追加</h2>
  <%= form_with model: [:admins, @couse], local: true do |f| %>
  <%= f.label :name, "コース" %>
  <%= f.text_field :name, placeholder: "ジャンル名を入力" %>
  <%= f.submit '新規登録' %>
  <% end %>

  <tr>
    <th>コース</th>
  </tr>

  <% @couses.each do |genre| %>
  <tr>
    <td><%= couse.name %></td>
    <td>
      %= link_to '編集する', edit_admins_couse_path(couse.id) %>
    </td>
  <% end %>